# Autonomous Workflow Guidelines

- **Autonomous Authority**: You have full permission to read, write, create, edit, test APIs, run migrations, and update database schemas across `backend`, `frontend`, and `OpenWA`.
- **Proactive Verification**: Immediately implement changes, run test scripts, build checks, and verify without pausing for user approval on routine modifications.
