# Autonomous Execution Rules

The user has explicitly granted full permission for autonomous end-to-end execution across this project:

1. **Full Read & Write Access**:
   - Create, edit, and refactor any source files in `backend`, `frontend`, `OpenWA`, and documentation.
2. **Database & Migrations**:
   - Perform SQLite / Prisma database schema updates, migrations (`npx prisma migrate` / `db push`), seed scripts, and queries directly.
3. **API & Automated Testing**:
   - Execute API calls, test suites, build checks, and integration tests proactively to verify functionality.
4. **No Blocking on Routine Actions**:
   - Implement solutions, bug fixes, and features directly without asking for confirmation before making edits.
